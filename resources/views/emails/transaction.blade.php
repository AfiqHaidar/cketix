<!DOCTYPE html>
<html lang="en">
<head>
    <title>Receipt</title>
</head>
<body>
    <p>{{ $ticket  }}</p>
    <p>{{ $transaction->total }}</p>
    <p>{{ $cat->code }}</p>
    <p>{{ $user->name }}</p>
</body>
</html>